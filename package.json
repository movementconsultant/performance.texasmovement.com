{
  "name": "performance.texasmovement.com",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Preview-only, non-interactive shell for Texas Movement Performance. Not the live site.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "postbuild": "node scripts/postbuild-guard.mjs",
    "preview": "astro preview",
    "check": "astro check",
    "test": "node --test tests/*.test.mjs",
    "test:a11y": "node scripts/a11y-scan.mjs"
  },
  "dependencies": {
    "astro": "7.2.2"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "@axe-core/playwright": "4.13.0",
    "playwright": "1.62.1",
    "typescript": "6.0.3"
  }
}
