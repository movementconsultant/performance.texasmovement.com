name = "performance-texasmovement-com"
compatibility_date = "2026-08-01"
pages_build_output_dir = "dist"

# Static Astro build (output: "static") — no adapter required for Cloudflare
# Pages. This file declares the Pages project shape only; it does not
# connect a domain, deploy, or change anything about the live site. Preview
# mode is controlled by the PUBLIC_PREVIEW build-time environment variable —
# see docs/LAUNCH_BLOCKERS.md.
